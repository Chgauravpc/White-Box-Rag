[pytest]
; Only backend/tests/ holds real pytest tests. This is intentional, not an
; oversight: backend/scripts/ holds manual, live-credential operator tools
; (e.g. manual_smoke_bp3.py) that must never be collected by pytest.
testpaths = tests
