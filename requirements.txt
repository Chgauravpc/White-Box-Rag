fastapi==0.115.0
uvicorn==0.30.0
openai==1.54.0
chromadb==0.4.24
numpy<2.0
sentence-transformers==3.0.0
# Pinned explicitly (not left to sentence-transformers' transitive resolution) —
# these determine NLI/embedding numerics; an unpinned upgrade is exactly the
# kind of silent nondeterminism the benchmark-readiness work is closing off.
torch==2.13.0
transformers==4.57.6
tokenizers==0.22.2
rank-bm25==0.2.2
scikit-learn>=1.3,<2.0
scipy>=1.10,<2.0
pydantic==2.9.0
python-multipart==0.0.9
pymupdf==1.24.0
python-docx==1.1.0
httpx==0.27.0
python-dotenv==1.0.0
aiofiles>=23.0.0
spacy==3.7.5
# After install, also run: python -m spacy download en_core_web_sm
# (installs the model version matching your spacy version — tested against
# en_core_web_sm 3.7.1). The model name itself is configurable via
# SPACY_MODEL in .env (see shared/config.py).
